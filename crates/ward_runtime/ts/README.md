# wardscript (npm)

The runtime for TypeScript and JavaScript modules compiled from Wardscript with
`ward build --target typescript`. See
[docs/spec/typescript.md](../../../docs/spec/typescript.md).

```bash
npm install          # TypeScript and Node types, for building and checking
npm run build        # dist/, plain JavaScript with type declarations
npm test             # the runtime's own tests
```
